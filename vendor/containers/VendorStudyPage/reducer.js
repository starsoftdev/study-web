/* eslint-disable comma-dangle, no-case-declarations */

const initialState = {
};

export default function vendorStudyPageReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
