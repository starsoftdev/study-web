// import { selectSearchByProtocolPageDomain } from '../selectors';
import expect from 'expect';

// const selector = selectSearchByProtocolPageDomain();

describe('selectSearchByProtocolPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
