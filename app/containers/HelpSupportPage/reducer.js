/*
 *
 * HelpAndSupportPage reducer
 *
 */

const initialState = {
};

function helpAndSupportPageReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default helpAndSupportPageReducer;
