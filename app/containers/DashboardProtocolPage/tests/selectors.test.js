// import { selectDashboardProtocolPageDomain } from '../selectors';
import expect from 'expect';

// const selector = selectDashboardProtocolPageDomain();

describe('selectDashboardProtocolPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
