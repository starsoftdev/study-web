// import { selectDashboardLockedUsersDomain } from '../selectors';
import expect from 'expect';

// const selector = selectDashboardLockedUsersDomain();

describe('selectDashboardLockedUsersDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
