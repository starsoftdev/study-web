// import { selectGlobalNotificationsDomain } from '../selectors';
import expect from 'expect';

// const selector = selectGlobalNotificationsDomain();

describe('selectGlobalNotificationsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
