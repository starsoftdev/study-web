
const initialState = {
  values: {
    filters: [],
  },
};

export default function dashboardPageReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
