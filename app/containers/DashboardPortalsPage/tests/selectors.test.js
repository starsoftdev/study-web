// import { selectDashboardPortalsPageDomain } from '../selectors';
import expect from 'expect';

// const selector = selectDashboardPortalsPageDomain();

describe('selectDashboardPortalsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
