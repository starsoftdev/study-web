// import { selectDashboardManageUsersDomain } from '../selectors';
import expect from 'expect';

// const selector = selectDashboardManageUsersDomain();

describe('selectDashboardManageUsersDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
