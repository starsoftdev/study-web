// import { selectReportViewPageDomain } from '../selectors';
import expect from 'expect';

// const selector = selectReportViewPageDomain();

describe('selectReportViewPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
