// import { selectReceiptsDomain } from '../selectors';
import expect from 'expect';

// const selector = selectReceiptsDomain();

describe('selectReceiptsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
