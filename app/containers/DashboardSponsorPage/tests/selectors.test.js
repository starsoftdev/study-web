// import { selectDashboardSponsorPageDomain } from '../selectors';
import expect from 'expect';

// const selector = selectDashboardSponsorPageDomain();

describe('selectDashboardSponsorPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
