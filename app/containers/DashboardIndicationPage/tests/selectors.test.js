// import { selectDashboardIndicationPageDomain } from '../selectors';
import expect from 'expect';

// const selector = selectDashboardIndicationPageDomain();

describe('selectDashboardIndicationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
