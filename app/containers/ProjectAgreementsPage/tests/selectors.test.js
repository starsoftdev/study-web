// import { selectProjectAgreementsPageDomain } from '../selectors';
import expect from 'expect';

// const selector = selectProjectAgreementsPageDomain();

describe('selectProjectAgreementsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
