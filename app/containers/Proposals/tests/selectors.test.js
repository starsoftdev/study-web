// import { selectProposalsDomain } from '../selectors';
import expect from 'expect';

// const selector = selectProposalsDomain();

describe('selectProposalsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
