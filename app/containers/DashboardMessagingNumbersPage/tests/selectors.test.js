// import { selectDashboardMessagingNumbersPageDomain } from '../selectors';
import expect from 'expect';

// const selector = selectDashboardMessagingNumbersPageDomain();

describe('selectDashboardMessagingNumbersPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect('Test case').toEqual(false);
  });
});
